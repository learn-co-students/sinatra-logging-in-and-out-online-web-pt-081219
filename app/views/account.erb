<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
     <% if Helpers.is_logged_in?(session[:user_id]) %>
     <h1>Welcome <%= Helpers.current_user(session[:user_id]).username%></h1>
     <h3>Your Balance: <%= Helpers.current_user(session[:user_id]).balance %></h3>
 <%end%>
<a href="/logout" >Log Out</a> 

  </body>
</html>